<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.eclipse.jetty</groupId>
    <artifactId>jetty-core</artifactId>
    <version>12.1.14-SNAPSHOT</version>
  </parent>
  <artifactId>jetty-util-ajax</artifactId>
  <name>Core :: Utilities :: JSON</name>
  <description>Jetty JSON Utilities Artifact</description>
  <properties>
    <bundle-symbolic-name>${project.groupId}.util.ajax</bundle-symbolic-name>
    <spotbugs.onlyAnalyze>org.eclipse.jetty.util.ajax.*</spotbugs.onlyAnalyze>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.eclipse.jetty</groupId>
      <artifactId>jetty-util</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>

    <dependency>
      <groupId>org.eclipse.jetty</groupId>
      <artifactId>jetty-slf4j-impl</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <executions>
          <execution>
            <id>default-testCompile</id>
            <configuration>
              <!-- RecordSupportTest compiles a record at runtime with javax.tools. -->
              <compilerArgs>
                <arg>--add-modules</arg>
                <arg>java.compiler</arg>
                <arg>--add-reads</arg>
                <arg>org.eclipse.jetty.util.ajax=java.compiler</arg>
              </compilerArgs>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>@{argLine} ${jetty.surefire.argLine} --add-modules java.compiler --add-reads org.eclipse.jetty.util.ajax=java.compiler</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
